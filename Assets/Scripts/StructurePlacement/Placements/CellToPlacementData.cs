using System.Collections.Generic;
using UnityEngine;

public class CellToPlacementData
{
    /*private static PlacementData CreatePlacementFromCell(CellWorldData cellWorldData)
    {
        var placementData = new PlacementData();
        
        placementData.SetTransformValues(cellWorldData.Position, cellWorldData.Rotation, cellWorldData.Scale);
        return placementData;
    }

    public static IEnumerable<PlacementData> GetCreatedPlacement(HashSet<CellWorldData> cellDatas)
    {
        //List<PlaceholderData> placeholders = new List<PlaceholderData>();
        
        foreach (var cellData in cellDatas)
        {
            yield return CreatePlacementFromCell(cellData);
            //placeholders.Add(Create(cellPosition, Quaternion.identity, Vector3.one));
        }
    }*/
}
